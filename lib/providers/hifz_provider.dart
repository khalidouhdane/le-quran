// ──────────────────────────────────────────────
// DEPRECATED — This provider is no longer used.
// Replaced by HifzProfileProvider in the new hifz program.
// Kept for reference only. Remove once migration is stable.
// ──────────────────────────────────────────────

import 'package:flutter/material.dart';

/// @deprecated Use HifzProfileProvider instead.
class HifzProvider extends ChangeNotifier {
  // Stub — no longer functional.
  HifzProvider();
}
